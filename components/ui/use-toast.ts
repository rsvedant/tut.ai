// Assuming this file already exists in the shadcn components
// If not, we would need to create it with the proper implementation
import { useToast as useToastShadcn } from "@/components/ui/toast";

export const useToast = useToastShadcn;
